import getJsonWithId from 'hackathon-team-4-frontend-permits/utils/get-json-with-id';
import { module, test } from 'qunit';

module('Unit | Utility | get-json-with-id', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = getJsonWithId();
    assert.ok(result);
  });
});
